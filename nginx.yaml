- name: nginx install
  hosts: frontend
  become: yes
  tasks:
    - name: install nginx
      ansible.builtin.dnf:
        name: nginx
        state: present

    - name: start nginx
      ansible.builtin.dnf:
        name: nginx
        state: started
        enabled: yes
