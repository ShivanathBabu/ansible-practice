- name: username
  hosts: frontend
  connection: local
  vars:
  - name: username
    prompt: please enter username
    private: true
  - name: password
    prompt: please enter password
    private: true

  tasks:
  - name: ping your information
    ansible.builtin.debug:
      msg: "hello username is {{username}} ans password is {{password}}"

